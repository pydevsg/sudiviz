# Core runtime — required for `sudiviz diagnose` / `drift` / `graph --output png`.
typer>=0.15.0
boto3>=1.35.0
botocore>=1.35.0
networkx>=3.0
pydantic>=2.0
rich>=13.0
aiohttp>=3.9
graphviz>=0.20
jinja2>=3.1

# Dynamic visualization — install via `pip install sudiviz[all]` for one-shot setup.
textual>=0.50.0     # `sudiviz tui`
fastapi>=0.115.0    # `sudiviz graph --output web`
uvicorn>=0.30.0
websockets>=13.0
diagrams>=0.23      # optional: nicer PNG iconography
python-terraform>=0.10  # optional: subprocess wrapper around terraform CLI

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5
mypy>=1.8
